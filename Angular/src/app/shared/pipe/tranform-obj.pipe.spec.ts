import { TranformObjPipe } from './tranform-obj.pipe';

describe('TranformObjPipe', () => {
  it('create an instance', () => {
    const pipe = new TranformObjPipe();
    expect(pipe).toBeTruthy();
  });
});
